spring:
  datasource:
    url: jdbc:mysql://localhost:3306/Health
    username: root
    password: dibya
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update      # automatically update DB schema on startup (use carefully in production)
    show-sql: true          # print SQL queries to console for debugging

boot:
  admin:
    client:
      url: http://localhost:1111  # Spring Boot Admin Server URL for client registration

server:
  port: 8081                    # Your application's HTTP port

management:
  endpoints:
    web:
      exposure:
        include: "*"             # expose all actuator endpoints over HTTP
